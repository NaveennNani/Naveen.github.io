$(function(){

//     $("#myh1").css({"color": "orange", "backgroundColor": "pink"})
//     $("#myp").next().css({"color": "green", "fontSize": "30px"})
//     $("#myh1").nextAll().css({"color": "orange", "backgroundColor": "pink"})
//     $("#myul").nextAll().css({"color": "orange", "backgroundColor": "pink"})
//     $("#myp").prev().css({"color": "green", "fontSize": "30px"})
//     $("#myul").prevAll().css({"color": "orange", "backgroundColor": "pink"})
//     $("#myp").after("<h2>This is because of After Method</h2>")
//     $("#myp").before("<h2>This is because of Before Method</h2>")
//     $("li").parent().css({"color": "green", "fontSize": "30px"})
//     $("li").parents().css({"color": "green", "fontSize": "30px"})
//     $("#myul").children().css({"color": "orange", "backgroundColor": "pink"})





//     $("h1").first().css("color", "red")
//     $("h1").last().css("color", "red")
//     $("h1").eq(1).css("color", "red")
//     $("div").eq(1).css("color", "red")
//     $("h1").filter("#thirdh1").css("color", "red")
//     $("h1").not(2).css("color", "red")





//     $("#myol").append("<li>MAGENTA</li><li>PURPLE</li>")
//     $("#myol").prepend("<li>MAGENTA</li><li>PINK</li>")
//     $("#myol").appendTo("<li>MAGENTA</li><li>PURPLE</li>") --> INVALID SYNTAX
//     Difference between append() and appendTo() is append() will take content as the argument, whereasm appendTo() will take selector as the argument 
//     $(selector).append(content)
//     $(content).appendTo(selector)
//     $("<li>MAGENTA</li><li>PURPLE</li>").appendTo("#myol")
//     $(content). prepedTo(selector)
//     $("<li>MAGENTA</li><li>PINK</li>"). prependTo("#myol")
//     append(), appendTo(), prepend(), prependTo(), after() and before()
//     $("#myol").after("<h1>I am from after method</h1>")
//     $("#myol").before("<h1>I am from before method</h1>")
//     $("#myol").after("<h1>I am from after method</h1>") 
//     $(selector).after(content)
//     $("<h1>I am from after method</h1>").insertAfter("#myol")
//     $("<h1>I am from before method</h1>").insertBefore("#myol")





//     Replace Elements and Content

//     $("#mypara").replaceWith("<a href='https://www.facebook.com'>Click Here</a>")
//     $("#myol").replaceWith("<ul><li>ORANGE</li></ul>")
//     $("li").replaceWith("<h1>Hello</h1>")
//     $("<a href='https://www.facebook.com'>Click Here</a>").replaceAll("#mypara")
//     The difference between replaceWith() and replaceAll() is, replaceWith() will take content as the argum // whereas replaceAll() will take selector as the argument


//     What is the difference between remove() and empty()
//     $("#container").remove() (div tag as well as the content present inside the div tag)
//     $("#container").empty() (remove the content present inside the div tag, but it will not remove the content





// adding and deleting attributes

//      let data = $("a").attr("href")
//      console.log(data)
//      $("a").attr("href", "https://www.amazon.in")
//      $("img").attr("src", "https://images.pexels.com/photos/376464/pexels-photo-376464.jpeg?cs=srgb&dl=pex.com")
//      $("img").attr("width", "500px")
//      $("img").attr("height", "700px")
//      console.log($("img").attr("height"))





})









// Moving from one HTML element to another HTML element is called as?
// traversing

// The tree which contains the represented HTML tags is called?
// DOM

// Which method is used to identify a particular child out of the many children present for a particular HTML element based on the index position?
// eq()

// Which method is used to identify the first child out of the many children present for a particular HTML element?
// first()

// Which method is used to identify the last child out of the many children present for a particular HTML element?
// last()

// Does appendTo() take an argument as the content?
// No

// Which method is used to add the element after the current HTML element that is selected?
// append()

// Which method is used to remove the element after the current HTML element that is selected?
// prepend()

// Which method is used to replace one HTML content with another HTML content?
// replaceWith()

// Which method takes the content as the argument?
// replaceWith()

// Which method takes the selector as the argument?
// replaceAll()

// Does remove() or empty() accept any arguments?
// No.

// Which method removes the child content along with the parent content?
// remove()

// Which method removes the child content but doesn’t delete the parent content?
// empty()

// How many arguments that attr() accept?
// 2

// The combination of attribute name along with attribute value is called ?
// attribute

// Which method is used to manipulate the attribute present with any HTML element?
// attr()





// MODULE DAY 43

// Does prependTo() take the argument as the content?
// No.

// Which method is used to create the HTML code and add it to the web page before the current HTML element?
// before()

// Which method is used to create the HTML code and add it to the web page after the current HTML element?
// after()

// Which method is used to identify all the next HTML elements that come after the current HTML element?
// nextAll()

// Which method is used to identify all the previous HTML elements that come before the current HTML element?
// previousAll()

// Which method is used to identify the next HTML element that comes after the current HTML element?
// next()

// Which method is used to identify the only parent of the current HTML that is selected?
// parent()

// Which method is used to identify the parent, grandparent, etc of the current HTML that is selected?
// parents()

// Which method is used to identify the previous HTML element that comes before the current HTML element?
// previous()





































































